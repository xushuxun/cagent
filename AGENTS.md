## 开发原则

基本原则
- 保持简单，严格分析各个模块和环节，无效、降低价值投资质量的设计，要删除
- 系统模块要正交, 证明有效的设计才能集成到系统
- 每个模块内部也要求小步实现，每步都要真实验证效果
- 多用AI，自适应分析，除非例外，否则禁止手写规则

测试与错误处理
- 防止llm错误累积，单次任务全流程端到端产出报告，避免留下临时缓存文件影响下一次任务
- 防止系统腐化，积累llm评估样例，作为测试例子补充到 cagent/tests 的脚本中
- cagent/tests 不是单元测试，为最终效果服务，测试例子最好是 llm 问答，以回答质量作为评判

文档与组织
- 文档与代码细节分离，文档记录原则和大方向指引，coding agent频繁修改代码，不要触发文档修改
- 仓库根即包根：模块一律绝对导入（`cagent.*`），禁用相对导入与手工 sys.path
- 入口、批处理、环节验证都是脚本，`uv run python <脚本>` 直接运行；用法写在各脚本
  docstring 里，改代码时随代码一起改，本文档不记命令
- 包内模块命名不得与标准库/第三方库同名（脚本直接运行时所在目录在 sys.path 首位，
  同名即遮蔽）

## 开发工具

- `uv add` 添加 python 依赖；
- `uv run` 执行 python 脚本
- `uv run ruff check --fix`
- `uv run ty check --fix` 代码检查（先自动修复再报剩余）

### Auto Research

- 遵循开发原则
- 每次实验要记录方案到 `EXPERIMENTS.md` ，方案通过后commit代码 
- `EXPERIMENTS.md` 不要记录细节，细节在commit代码中，记录大的方案设计以及实验收获教训
- 方案要有消融实验
- 不要并发跑方案，一次只优化一件事情


方法一：端到端pipeline：分别跑同家公司的A股和港股年报，比对报告，然后分析报告和trace，改进

方法二：小范围测试：输入md和toc，运行 cagent/tests/ 下的环节验证脚本，独立验证单个环节，注意脚本中管理llm服务



## 目录地图

```
cagent/        # 分析主体（项目包）：pipeline 编排、agent、各环节包、环节验证
lakehouse/     # 存储规范与股票池名单（布局约定见其 README）
templates/     # 报告模板（能力状态见 README）
reports/       # 报告产出与 trace
output/        # 服务日志与互斥锁
```

公告本体在仓库根 `.cagent/`（数据目录，默认 lakehouse 根，可被参数覆盖），布局以 `lakehouse/README.md` 为准。

## 架构总览

数据在环节之间单向流转，每环节产物是下一环节的输入。只有两类构建产物允许落盘
（解析全文、章节目录，与原文同级、可重生成）；环节中间态一律不落盘，重跑即重算。

```
数据目录 <仓库根>/.cagent              产出 reports/<市场>/<代码>/
  raw/   原始 PDF + 公告索引                   report.md（报告正文）
  derived/ 解析全文 + 章节目录               meta.json（异常清单，空 = 无异常）

[下载] → raw PDF → [解析] → 全文（物理页码标记，失败页占位）→ [章节目录]
                                 ｜                            ｜
     ┌───────────────────────────┘                            ｜
     ▼                                                         ▼
[定量] 表格直解 → 数字表（唯一数字来源）              [定性] 按目录选章 → 章节摘要 + 管理层叙事
     ｜  恒等式 / 聚合行 / 资产段机械校验                      （每条结论带页码出处）
     ▼                                                         ▼
[估值] 锚表 / DCF矩阵 / 盈利质量对账 / 清算价值      [证据合并] 摘要 + 锚表 → 结构化证据 JSON
     ｜  数字表格全部程序计算渲染，模型只出折扣率
     └──────────────────┬──────────────────────────┘
                        ▼
                 [装配] 模板 + 证据 + 程序表格 → 报告正文（占位符替换，模型不制表）
                        ▼
                 [对账] 报告 vs 证据 → 只检测不改写
                        ▼
                 report.md + meta.json
```

环节职责与机械防线：

| 环节 | 职责边界 | 机械防线 |
|---|---|---|
| 下载 | 抓取年报入库，幂等去重 | raw 层不改写，索引与文件一致 |
| 解析 | PDF → 全文 | 失败页占位可被检出；页码连续无缺号 |
| 章节目录 | 章节标题/页码区间/一句话摘要 | 结构自检；全文覆盖、每页恰一次 |
| 定量 | 数字表是财务数字唯一出处 | 恒等式逐年、行冲突只披露、聚合行 = 前后若干行之和 |
| 定性 | 选章摘要与叙事，全部带页码 | 必备结构齐全；「未披露」可回原文核验 |
| 证据合并 | 跨公告合并为结构化证据 | 顶层结构齐全，矛盾进数据缺口 |
| 估值 | 数字表格全部程序计算渲染 | 上界对账、折扣率界内、清单覆盖披露 |
| 装配 | 模板 + 证据 → 报告，搬运不改写 | 占位符就位、异常全进异常清单 |
| 对账 | 独立上下文复核，只检测不改写 | 检出一律披露；未检出 ≠ 无异常 |

服务与接入：本地推理服务（文本 / OCR）由统一服务层管理生命周期，端到端与环节验证共用
同一套启动命令与回收规则；LLM 接入统一走带 trace 的通道，结果进表格或被机械核对的
调用一律确定性解码。每环节一个验证脚本（离线合成用例 + 真实年报档），排障从上游往下，
第一个 FAIL 即本轮目标；端到端用同一家公司 A/H 两地报告互验（数字表格逐格一致、
没有假陈述）。
