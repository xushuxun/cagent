# 实验记录

只记**大的方案设计**与**实验收获教训**；细节（改了什么函数、什么数字）在 commit 的
代码里。验收标准与跑法见 `AGENTS.md`。

## 下载环节建立验收验证（离线一致性 + 真实下载幂等，两档）

给下载环节补了独立验证脚本，覆盖同一家公司 A/H 两地（cn 601633 与 hk 02333）：
离线档零网络零写，核对本地数据目录的索引—磁盘一致性、命名规范、临时残留；
live 档把真实下载放到临时目录（近 2 年窗口）并反复刻幂等。

**收获：** 下载环节的 happy path 是健康的——两家真实公司近 2 年年报能下、重跑不覆盖；
之前审计发现的缺陷集中在 force 清理与退出码语义上，不在基本下载链路里。这给后续
「一次只优化一件事」定了位：先修异常路径，而不是怀疑主链路。

## 环节验证为最终效果服务，不是单元测试

环节验证脚本不测内部函数、不测中间实现细节；它们为最终效果负责——跑完整流水线（生产
入口、真实或临时数据），从 lakehouse 的构建产物里捞结果，对照 AGENTS 架构总览里每个
环节的机械防线做断言（页码连续、grounding 零残留、恒等式成立、逐格一致、幂等……）。

**收获：** 面向内部函数的用例在实现重构后成批失效，且不指向任何用户可见质量；面向
产物的断言在重构后依然有效、直接对应验收标准。验证跟着「最终效果」走，重构才不心疼，
也才不会把测试变成另一套需要维护的实现。
## 定量：从「只提主表」到「全量财务表格 + 可验证性标签」

概念数字表只留 19 个概念锚点，年报里 200 多张附注/指标/经营表全部被丢掉。本轮把定量改成
**逐张直解并结构化**，概念数字表退化成这次直解的一个投影；每张表的每个值列带可验证性标签，
标签由算术证据测得、不由模型声明：tied（附注合计与主表同科目同期间勾稽上）/ sum（表内
合计恒等式成立）/ unverified（说明性表格，无会计勾稽可验，不得当已验证财务数字）。

**收获：**
- 手写规则不能当解析器。报表期推断那套正则（中文数词表、标题写法枚举、"公告年份-1"兜底）
  整块删除——报表期年份本来就印在主表列头上，两地五年共 10 份真实年报都能直接读出本期与
  上年同期，不需要推断。
- OCR 保留了 `colspan`/`rowspan`。此前判定"跨列表头无法归属"并把列语义交给模型誊抄（实测
  一批 25 表 220 秒且信息无增益）；改为按合并跨度摊平后，列的期间与语义直接可读，多行表头
  列名覆盖从 0 张升到 300/325 张，模型职责缩到「这张表讲什么、是不是财务口径、合并还是
  母公司、单位与币种」。语义留给模型，算术与结构留给程序。
- 表格外的文字是判据的一部分：表后的「注：…」限定条件只截表前会被记到下一张表头上。
- 口径差异不是异常。附注合计与主表不等的地方全是毛额/净额、期初/期末、余额/准备之类合法
  口径，逐条报会把 meta 变噪声；只汇总一条，相等才升格为 tied。
- 验证要跑真实数据、要用 LLM 问答。装配前的环节全用合成表格自证，会既测不出真缺陷也守不住
  质量：同一份年报，结构自检全绿而真实问答只过三分之一；合成用例里两条 GT 本身就是错的。
- 先确认服务真在用 GPU 再谈吞吐：CUDA 掉线时一份年报全量标注 40+ 分钟，恢复后约 4.4 分钟，
  期间的"成本危机"其实是环境故障。
